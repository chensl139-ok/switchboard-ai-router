export function renderPrices({state,api,esc,toast,onSaved}){
 const root=document.querySelector('#content');
 root.innerHTML=`<div class="heading"><div><div class="eyebrow">MODEL PRICING</div><h1>模型价格</h1><p>按实际调用服务商维护价格；未知或过期价格不参与经济优先。</p></div></div><div class="panel"><label>服务商<select id="price-provider">${state.providers.map(p=>`<option value="${esc(p.id)}">${esc(p.name)}</option>`).join('')}</select></label><button id="sync-prices">从 OpenRouter 同步价格</button><p class="muted">自动价格仅适用于 OpenRouter 这一调用渠道，单位为 USD；其他服务商可按实际合同手动维护。</p><div id="price-table"></div></div>
 <div class="panel section-space"><h2>录入 / 更新价格</h2><form id="price-form"><label>模型<select name="model" id="price-model" required></select></label><div class="form-grid"><label>币种<select name="currency"><option>USD</option><option>CNY</option></select></label><label>价格有效期至<input name="expiresAt" type="datetime-local"></label><label>输入价格 / 百万 Tokens<input name="inputPerMillion" type="number" min="0" step="any" required></label><label>输出价格 / 百万 Tokens<input name="outputPerMillion" type="number" min="0" step="any" required></label><label>每次请求固定费用<input name="perRequest" type="number" min="0" step="any" value="0"></label></div><button class="primary">保存价格</button></form><p class="muted">留空有效期默认 30 天；自动同步默认有效 7 天。0 表示已确认免费，空白不表示免费。</p></div>`;
 const provider=()=>state.providers.find(p=>p.id===root.querySelector('#price-provider').value);
 function draw(){const p=provider();root.querySelector('#sync-prices').disabled=p.baseUrl!=='https://openrouter.ai/api/v1';root.querySelector('#price-model').innerHTML=p.models.map(m=>`<option>${esc(m)}</option>`).join('');root.querySelector('#price-table').innerHTML=`<div class="table-wrap"><table><thead><tr><th>模型</th><th>输入 / 输出（百万）</th><th>来源</th><th>有效期</th></tr></thead><tbody>${p.models.map(m=>{const price=p.prices?.[m];return `<tr><td>${esc(m)}</td><td>${price?`${esc(price.currency)} ${price.inputPerMillion} / ${price.outputPerMillion}`:'未知'}</td><td>${price?esc(price.source):'—'}</td><td>${price?esc(new Date(price.expiresAt).toLocaleString()):'—'}</td></tr>`;}).join('')}</tbody></table></div>`;}
 root.querySelector('#price-provider').onchange=draw;draw();
 root.querySelector('#sync-prices').onclick=async e=>{e.target.disabled=true;try{const s=await api('/api/prices/sync',{providerId:provider().id});onSaved(s.state);toast('已同步 '+s.count+' 个已配置模型的价格');}catch(error){toast(error.message);e.target.disabled=false;}};
 root.querySelector('#price-form').onsubmit=async e=>{e.preventDefault();const b=Object.fromEntries(new FormData(e.target));b.providerId=provider().id;for(const field of ['inputPerMillion','outputPerMillion','perRequest'])b[field]=Number(b[field]);if(b.expiresAt)b.expiresAt=new Date(b.expiresAt).toISOString();else delete b.expiresAt;const button=e.target.querySelector('button');button.disabled=true;try{onSaved(await api('/api/prices',b));toast('价格已保存');}catch(error){toast(error.message);button.disabled=false;}};
}
