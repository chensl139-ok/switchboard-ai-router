# 账户、租户与路由控制

## 首次升级

使用 `npm start` 或当前 Docker 镜像启动，入口是 `platform.mjs`。
首次访问需要提供部署时 `.env` 中的 ADMIN_TOKEN，并设置姓名、邮箱和密码（12–256 字符）。初始化只允许一次。旧服务商配置和业务 Key 保留在默认租户，由第一个所有者账户接管；不自动创建任何真实用户。

之后使用账户登录，管理端使用 12 小时有效的 HttpOnly、SameSite=Strict Cookie。会话、邀请码和密码均只存哈希，密码使用带随机盐的 scrypt。修改密码撤销该账户其他会话。没有开放匿名注册，管理员生成与邮箱绑定的 72 小时邀请码，由用户自行通过安全渠道交付；系统不发送邮件。

## 角色权限

| 操作 | 所有者 | 管理员 | 成员 | 只读 |
| --- | --- | --- | --- | --- |
| 查看租户模型、配置、用量与调用日志 | 是 | 是 | 是 | 是 |
| 实验室调用模型 | 是 | 是 | 是 | 否 |
| 服务商、规则、价格、API Key 管理 | 是 | 是 | 否 | 否 |
| 邀请与管理非所有者成员 | 是 | 是 | 否 | 否 |
| 授予所有者、管理所有者 | 是 | 否 | 否 | 否 |
| 创建自己的新租户 | 是 | 是 | 是 | 是 |

角色按租户分配，同一账户在不同租户可以拥有不同角色。不可删除或降级最后一个所有者。移除成员和降级角色后，每次 HTTP 或 WebSocket 新请求重新鉴权；已开始的生成允许完成。

## 隔离边界

默认租户使用原 data 目录，新增租户使用服务端生成的 UUID 子目录，每个租户有自己的主密钥、提供商配置、Key 配额文件和 SQLite 用量库。
外部 API Key 在服务端绑定租户，不接受客户端指定租户来跨越权限。会话由服务端记录当前租户。浏览器额外发送 X-Tenant-ID 作为一致性检查，切换租户后旧页面的请求返回 409；该 Header 不能改变授权范围。WebSocket 连接固定在认证时的租户，切换后必须重连。

旧 GATEWAY_TOKEN 仅绑定默认租户，可在 Key 管理中关闭。新租户没有可分发的环境令牌，必须由管理员创建专用 Key。
删除 Key 使用不可恢复的吊销标记：任何新请求都被拒绝，不能重新启用；已生成的日志与计数保留。活动 Key 上限为每租户 200 个。

当前为单实例、最多 50 个租户。配置和账户使用原子文件替换，用量使用 SQLite WAL。不可把多个进程/副本指向同一目录；物理宿主机管理员仍能读取数据，不属于租户隔离威胁边界。

## 模型价格与经济优先

自动同步仅使用 OpenRouter 官方 `https://openrouter.ai/api/v1/models` 返回的结构化 prompt/completion/request 价格，转换为美元/百万 Tokens。该价格只适用于 OpenRouter 调用渠道，不会套用到 DeepSeek、硅基流动等直连渠道。
截至实现时，未确认这些直连渠道公开模型列表提供完整结构化价格，故使用手动维护而非网页抓取。自动价格 7 天过期，手动价格默认 30 天过期。所有价格都有币种、来源、更新时间与有效期。

经济优先在租户启用的服务商及其已保存模型中选择。输入 Token 数使用 UTF-8 字节数/3 向上取整做粗略估算；输出使用调用方实际设置的 max_tokens（默认 2048）。比较输入 + 输出 + 固定请求费用，价格相同时按优先级排序。只比较所选币种 USD 或 CNY，不做隐式汇率转换。未知、负数、过期或不同币种价格不参与；0 只表示上游或管理员明确提供的免费价格。仍遵循熔断、最大尝试次数、总超时和思考能力检查。

此策略不保证真实账单绝对最低：实际 Token 数、缓存折扣、时段价格、服务商路由差异和额外收费会改变结果。用量页费用基于请求开始时的价格与上游实际报告的输入/输出用量估算，不等同于账单。上游不返回用量时标记未知，不计为零费用。

官方参考：[OpenRouter 模型和价格接口](https://openrouter.ai/docs/api/api-reference/models/list-all-models-and-their-properties)。

## 日志与审计

SQLite 保存最近 90 天每次上游尝试的请求 ID、Key ID、调用者 ID、服务商、模型、状态、耗时、已知输入/输出 Tokens、估算费用和路由依据。相同生成请求的内部回退共享请求 ID。
用量页区分生成请求数和上游尝试数，按 UTC 日聚合、按服务商/模型/Key 分组，并分币种统计费用。旧版最近 500 条日志仅导入一次，没有输入输出拆分时不会推算费用。
不保存提示词、正文、思考文本、密码或明文密钥。成员与配置变更另记租户审计。SQLite 中的删除与日志保留按产品规则执行，备份整个数据目录前应停止应用，确保 WAL 与数据库文件一致。

## 部署与安全设置

- 公网必须使用 HTTPS；设置 COOKIE_SECURE=true。公共 Compose、Render、Fly 配置已启用。
- 默认拒绝私网和保留地址上游，并在实际连接时固定经过检查的 DNS 结果。内部推理服务只能由部署管理员通过 UPSTREAM_ALLOWED_PRIVATE_HOSTS 精确授权，HTTP 还需 ALLOW_HTTP_UPSTREAM=true。
- 若本机使用 Fake-IP 代理，可显式设置 UPSTREAM_PROXY_FAKE_IP=true；仅兼容预置官方服务域名的 198.18/15 虚拟地址，TLS 证书校验仍开启。自定义域名和普通私网不享受此例外。
- 登录/注册有按来源 IP 的速率限制；代理后的多个用户可能共享来源限额。此版本未实现 MFA、SSO、邮箱验证、邮件找回密码、自定义角色或分布式限流。
