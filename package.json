{
  "name": "switchboard-ai-router",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test test/*.test.mjs",
    "setup": "node scripts/setup.mjs",
    "deploy": "node scripts/deploy.mjs",
    "deploy:public": "node scripts/deploy.mjs --public",
    "check": "node --check server.mjs && node --check key-store.mjs && node --check realtime.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "description": "Independent multi-provider AI router with SSE, WebSocket and API key quotas"
}
