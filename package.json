{
  "name": "switchboard-ai-router",
  "version": "3.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env platform.mjs",
    "test": "node --test test/*.test.mjs",
    "setup": "node scripts/setup.mjs",
    "deploy": "node scripts/deploy.mjs",
    "deploy:public": "node scripts/deploy.mjs --public",
    "check": "node --check platform.mjs && node --check server.mjs && node --check protocols.mjs && node --check protocol-stream.mjs && node --check model-catalog.mjs && node --check public/app.js && node --check public/model-catalog.js && node --check public/playground.js && node --check openapi.mjs && node --check public/api-docs.js && node --check media.mjs && node --check public/media-lab.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "description": "Independent multi-provider AI router with SSE, WebSocket and API key quotas",
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "openai": "^7.15.0"
  }
}
