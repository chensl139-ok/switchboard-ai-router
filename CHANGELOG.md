# Changelog

本项目遵循语义化版本。发布日期均使用北京时间。

## [2.0.3] - 2026-09-25

- 统一全站页面标题字号和外层边距；各二级标签栏统一置于标题后，对话与媒体页共用标题、标签栏及水平边界。重新规划对话页：消息区随窗口高度自适应，输入区贴底，聚焦时保持稳定的单层描边，生成设置按需展开，窄屏改为侧边浮层。
- 租户操作审计提供成员、API Key 和管理操作切换；管理操作支持操作人、类别、时间与关键词组合查询，按保留的完整事件集分页，详情展示事件标识与目标。
- 请求日志提供时间、服务商、状态、模型／服务商／请求 ID／路由依据及 API Key ID 组合筛选；可追踪一个请求的全部上游尝试并展开用量、费用与路由详情。
- API Key 创建与修改弹窗采用固定标题、关闭按钮和操作栏，长表单内容独立滚动，避免标题被裁切。
- 补充审计租户隔离、权限、超过 200 条后的历史查询、日志组合过滤和精确故障转移请求追踪回归测试。

## [2.0.2] - 2026-09-25

- 修复服务商模型选择的批量交互：模型列表改为紧凑勾选，搜索结果使用同一个按钮切换全选与取消全选，手动模型 ID 收入高级设置；服务商序号不再覆盖文案。
- 优化模型实验室对话、媒体和对比工作区的视觉层级、操作密度与状态反馈；将对话状态收进主工作区，去掉媒体页重复摘要。改进折叠导航、租户切换提示、主题图标、手机端顶部栏及服务商配置弹窗的固定标题。
- 修复服务商调用健康状态与日志筛选反馈；服务商卡片自动刷新，不需手动重载页面。组织审计只记录真正的配置变更，路由预览与模型获取不再误报为配置更新。
- OpenRouter 参考价格可批量导入其他服务商，补充硅基流动等 Hugging Face 原始模型 ID 匹配和高精度小数；手动或服务商原生价格优先。将价格采集时间与平台七天复核期限分开呈现，避免误称上游价格有效期。
- 改进对话路由：鉴权失败后跳过使用同一密钥的其他模型，将尝试预算留给其他服务商；媒体模型不进入对话故障转移。首页候选链改为读取真实路由预览，上线检查仅将成功调用计为完成。
- 补充跨服务商、同服务商故障转移、双密钥清除、价格导入、日志过滤与首页路由展示的回归测试。

## [2.0.1] - 2026-09-24

- 将仓库维护联系邮箱统一为 `15652641985@163.com`，并同步更新 README 与新版本发布产物。
- 修复 Release 工作流读取带日期的更新记录标题时清空发布说明的问题；找不到说明时停止发布，避免覆盖现有说明。

## [2.0.0] - 2026-09-24

- 故障转移候选优先覆盖首选服务商默认模型、同服务商另一模型、下一服务商默认模型；在默认三次预算内同时具备两级回退，保留备用密钥和熔断支持。
- 将协议与请求能力检查前移至尝试次数截断前；自动对话跳过媒体模型，不兼容候选不占用实际调用次数。
- 新增路由预览：展示候选顺序、协议、健康状态、尝试预算与过滤原因；预览不调用上游且不返回密钥。
- 优化 API 文档导航、接口搜索、实际 OpenAPI 接口同步及调用示例；修复折叠侧栏展开图标重复显示。
- 增加跨服务商、同服务商、思考开关、媒体过滤的 HTTP 集成测试与可选真实调用 smoke 脚本。
- 修复切换服务商模型后页面渲染空白；所有配置修改接口返回一致的安全状态快照。
- 模型实验室选定模型时显式启用同服务商及跨服务商容灾，不受后台首选排序策略限制；精确 `provider::model` 仍保持固定模型语义。
- TTFT 改用统一单调时钟并以首个真实输出增量为准；TPOT/Tokens/s 只在上游返回输出 Token 用量时计算，避免把 SSE 帧数冒充 Token 数。
- 流式请求在首个正文或工具输出前暂存角色/思考片段；若上游此时中断，可丢弃未提交片段并安全切换模型，避免展示失败的半截回答。
- 修复服务商健康卡片未识别 SQLite 日志的 `provider_id` 字段，已调用的服务商不再错误显示“待验证”。
- 自动路由或显式启用回退时，单个上游模型返回 HTTP 400 可继续尝试其他模型；禁用当前默认服务商后会自动选择下一个可用服务商，不再让默认路由指向禁用项。
- 就绪探针校验用量数据库完整性；数据库损坏时不再误报容器健康。已补充损坏场景回归测试。
- 服务商卡片每 10 秒拉取调用日志并就地更新健康状态，切换回页面时立即刷新，无需重载或打断筛选操作。
- 侧边栏主入口标出对应二级模块，面包屑同步显示层级；收起后租户头像保持导航轴线，租户列表在侧边弹出。
- 实验室 Tokens/s 改为基于上游实际输出 Token 与完整请求耗时的端到端吞吐，并明确标注口径；不再从 SSE 帧间隔推断不可靠的解码 TPOT。

## [1.2.0] - 2026-09-24

### 交互与界面

- 控制台导航合并：「服务商管理」与「模型目录」合并为「服务商与模型」（二级标签切换），「模型实验室」与「媒体实验室」合并到同一入口；侧边栏更干净。
- 新增全局搜索（按 `/` 聚焦，Esc 清除）和明暗主题切换（自动记住选择）。
- 控制台改为冷色科技主题，深色模式统一语义色板，并支持首次访问跟随系统外观；折叠侧栏移除右侧竖线，修复租户按钮溢出与图标对齐。
- 用量分析改为连续日期趋势、上游成功率、费用覆盖范围和可搜索模型明细；模型实验室将页面标签与操作放在同一行，让输入框更容易在首屏使用。
- 收拢页面路由与角色权限判断，分析视图拆分为可复用的渲染函数；新界面样式集中在 `public/interface.css`。
- 服务商配置弹窗：主密钥与备用密钥区分开，备用密钥折叠暂存；关闭前检测未保存修改并确认，避免误点丢失输入。
- 「账户与租户」支持租户所有者删除空租户，同时清理数据、邀请、API Key 与数据目录。

### 性能

- 首次渲染提速：静态资源走 gzip 预压缩 + ETag，内置 `/build/*` 一年 immutable 缓存；设置 `no-cache` 的入口 HTML 二次刷新近乎瞬时。
- 路由热路径降负载：健康/熔断判断从全部日志全量扫描改为 SQLite 查询 + 30 秒缓存，高并发时延明显改善。
- 状态文件瘦身：`state.json` 不再保存 500 条日志，启动迁移且仅保留 3 个历史备份。

### 可靠性

- 持久化串行化 + 立即回滚，彻底规避并发写损坏；进程级 `unhandledRejection`/`uncaughtException` 记录。
- HEAD 请求按规范返回合法响应；WS 心跳随服务端关闭而清理。

## [1.1.1] - 2026-09-24

### 性能

- 页面打开速度：静态资源（`index.html`/`app.js`/`style.css` 等）现支持 gzip 响应 + `ETag`/`If-None-Match` 304 缓存；`/build/assets/*` 使用 1 年 immutable 缓存。
- 路由热度：路由健康/延迟判断不再每次线性扫描 `state.logs`（SQLite 查询 + 30 秒内存缓存），高并发时请求时延小一个数量级。
- 状态瘦身：`state.json` 不再随请求保存 500 条日志，写盘尺寸从 ~93KB 降到常量级，写盘改为微任务串行。

### 可靠性

- `accounts`/`api-keys`/`state` 持久化失败时立刻回滚并抛出错误，杜绝状态与文件不一致。
- 进程退出与套接字清理：`platform`/`server` 关闭时清理周期性定时器与 WebSocket 心跳；进程新增 `unhandledRejection`/`uncaughtException` 记录。
- HEAD 请求按规范返回同 GET 头但不含 body；避免 curl -I 误判 404。

### 运维

- 启动时自动清理 3 个之前的 `state.json.backup-*`，旧版升级后 data 目录不再持续堆积。

### 其它

- 服务商配置弹窗关闭/取消时若有未保存修改会先确认，避免误关；切换租户时下拉框旁显示「数据与密钥按租户隔离」提示。

## [1.1.0] - 2026-09-24

### 新增

- 飞书企业 OAuth 登录、邀请加入、备用密码、成员角色和租户切换。
- 组织审计：按成员、API Key 和租户统计调用量、Token、费用与最近使用时间。
- 自定义服务商、模型自动协议匹配，以及同服务商多模型和备用密钥故障转移。
- 模型实验室实际路由信息，显示命中的服务商、模型、协议、策略与故障转移次数。
- 媒体实验室的视频生成、异步进度、图片和音频能力。
- 多模型对比、思考内容、图片输入、函数工具及流式草稿编辑。

### 改进

- 重构路由策略、健康检查、熔断、超时和失败原因记录。
- 全平台界面密度、服务商卡片、导航收起交互与响应式布局。
- 生产安全响应头、就绪探针、请求 ID、数据隔离和密钥加密。
- Docker 运行、只读文件系统、健康检查与多架构镜像发布流程。

### 兼容性

- 保持 OpenAI Chat Completions、Responses、Legacy Completions 和 Anthropic Messages 接口兼容。
- 数据目录可从 `v1.0` 原地升级；升级前仍建议完整备份 `data/`，尤其是 `master.key`。

## [1.0.0] - 2026-09-21

- 首个可部署版本：服务商管理、基础模型路由、API Key 和控制台。

[1.1.0]: https://github.com/chensl139-ok/switchboard-ai-router/releases/tag/v1.1.0
[1.0.0]: https://github.com/chensl139-ok/switchboard-ai-router/releases/tag/v1.0
